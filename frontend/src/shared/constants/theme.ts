import { colors } from "@/shared/constants/colors";
import { spacing } from "@/shared/constants/spacing";

export const theme = {
  colors,
  spacing,
  radius: {
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    pill: 999
  }
} as const;

